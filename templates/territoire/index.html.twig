{% extends 'back/base.html.twig' %}
{% block title%} Liste des territoires {% endblock %}
{% block body %}
    {% if territoire %}
    <section class="content">
      <div class="container-fluid">
        <div class="row">
        <div class="col-md-6">
            <div class="card card-info">
              <div class="card-header">
                <h3 class="card-title">Tous les territoires</h3>
              </div>
            <div class="card-body" >
           <a href="/territoire/new" class="btn btn-secondary">Ajouter un territoire</a>  
            </div>
        <div class="card-body">
                <table class="table table-bordered">
                <tr>
                 <th>Id</th>
                 <th>Nom</th>
                 <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                {% for opp in territoire %}
                    <tr>
                        <td>{{ opp.id }}</td>
                        <td>{{ opp.nom }}</td>
                        <td>            
                             <a href="/territoire/delete/{{ opp.id }}" class="btn btn-danger"onclick="return confirm('Etes vous sûr de supprimer ce departement?');">Supprimer</a>

                        </td>
                    </tr>
                {% endfor %}
             </tbody>
         </table>
        </div>
{% else %}
        <h1 align='center'>Aucun territoire</h1>
        <div align='center' >
           <a href="/territoire/new" class="btn btn-dark">Ajouter </a> </div>
        {% endif %}
{% endblock %}